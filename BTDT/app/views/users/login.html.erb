<%# <% byebug %>
<p><%= alert %></p>

<%= form_for current_user, url: login_path, method: 'POST' do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :password %>
    <%= f.password_field :password %>

    <%= f.submit "Login" %>
<% end %>
